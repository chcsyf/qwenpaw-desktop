# 变更记录

## [0.1.1] — 2026-08-08

功能增强：
- 📷 **一键截图**：右下工具新增「📷」按钮，截取远程桌面当前画面并弹窗预览，
  支持**下载 PNG** 与**保存到平台公共数据目录**（`plugin_data/screenshots/`，
  自动按时间命名；公共持久，不属于某个智能体）
- 📋 **剪贴板互通（修复中文乱码 + 选中即复制）**：
  - 改用 xclip 读写远程 X 桌面的 **CLIPBOARD** selection（UTF-8），绕开 VNC
    剪贴板协议的 Latin-1 编码——**中文不再乱码**
  - 只读 CLIPBOARD（Ctrl+C 语义），不碰 PRIMARY——**不再出现"鼠标选中文字
    就自动复制"**
  - 点「📋」写入远程剪贴板后**自动粘贴**（终端类窗口自动用 Ctrl+Shift+V，
    其他用 Ctrl+V）
  - 系统依赖新增 `xclip`（见 README.md）
- 🖥 **分辨率切换**：右下工具新增「🖥」按钮，可在 1280x720 / 1440x900（默认）/
  1920x1080 间一键切换（重启桌面栈生效，已打开的窗口会关闭并自动重连）
- 系统依赖新增 `scrot`（截图功能需要，见 README.md）

打开入口不变：**应用 → 远程桌面** 🖥️

---

## [0.1.0] — 2026-08-07（正式版）

第一个正式版本。打开入口：**应用 → 远程桌面** 🖥️

更新内容：
- ⚠️ **安装后请先刷新页面**：从插件市场安装后，若打开应用提示"该应用尚未加载，
  请先从侧边栏打开一次后重试"，刷新浏览器页面（F5 / Ctrl+Shift+R）后重新进入
  **应用 → 远程桌面** 🖥️ 即可正常使用（前端需重新加载插件 JS 以注册应用路由）

功能：
- 纯桌面视图（无浏览器工具栏），右下角竖排快捷入口：GitHub / Google / Bing / 百度 / 终端 / 文件
- 底部最小状态条：连接状态、重连、关闭桌面
- 桌面端物理键盘直接可用；远程 X 桌面强制开启 NumLock，数字小键盘正常
- 适合访问本地打不开的网站（如 github）：在服务器桌面里以 chromium 窗口打开

---

## [0.0.1] — 2026-08-07（首个发布版本）

首个发布版本。专用于 `qwenpaw.platform.agentscope.io` 平台。
应用启动入口：**应用 → 远程桌面** 🖥️

⚠️ 安装前需在服务器手动安装系统依赖包（插件不会自动安装系统包）：
`xvfb`、`openbox`、`x11vnc`、`xdotool`、`x11-xserver-utils`、`novnc`、
`xfce4-terminal`、`thunar`、`chromium`（详见 README.md）。

功能：
- 纯桌面视图（无浏览器工具栏），右下角竖排快捷入口：GitHub / Google / Bing / 百度 / 终端 / 文件
- 底部最小状态条：连接状态、重连、关闭桌面
- 桌面端物理键盘直接可用；远程 X 桌面强制开启 NumLock，数字小键盘正常
- 适合访问本地打不开的网站（如 github）：在服务器桌面里以 chromium 窗口打开
